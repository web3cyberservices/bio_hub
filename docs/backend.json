{
  "entities": {
    "UserProfile": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "UserProfile",
      "type": "object",
      "description": "Stores core information about a user, including biometrics, habits, and medications.",
      "properties": {
        "id": { "type": "string" },
        "email": { "type": "string", "format": "email" },
        "firstName": { "type": "string" },
        "lastName": { "type": "string" },
        "displayName": { "type": "string" },
        "photoUrl": { "type": "string" },
        "birthDate": { "type": "string", "format": "date" },
        "gender": { "type": "string" },
        "weight": { "type": "number" },
        "height": { "type": "number" },
        "activityLevel": { "type": "string" },
        "healthGoal": { "type": "string" },
        "profileType": { "type": "string", "enum": ["user", "specialist"] },
        "specialization": { "type": "string" },
        "bio": { "type": "string" },
        "instagramUrl": { "type": "string" },
        "telegramChatId": { "type": "string", "description": "ID чата в Telegram для отправки уведомлений." },
        "medications": { "type": "string", "description": "Medications or supplements user is taking." },
        "sharedWith": { "type": "array", "items": { "type": "string" } },
        "lastHealthSync": { "type": "string", "format": "date-time" },
        "createdAt": { "type": "string", "format": "date-time" }
      },
      "required": ["id", "firstName", "profileType"]
    }
  }
}