rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // ПОЛНЫЙ ДОСТУП ДЛЯ ТЕСТИРОВАНИЯ
    // Разрешаем чтение и запись для всех коллекций и вложенных документов
    match /{allPaths=**} {
      allow read, write: if true;
    }
  }
}