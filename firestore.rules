rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // ПОЛНЫЙ ДОСТУП ДЛЯ ТЕСТИРОВАНИЯ
    // Это правило разрешает любые операции чтения и записи без ограничений.
    match /{allPaths=**} {
      allow read, write: if true;
    }
  }
}